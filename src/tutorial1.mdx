import { Overview } from './sections/Overview'

# MDX Linting Demo

1. item one
2.  item two

```
	const thing = true;

	if (thing) {
		console.log('thing');
	}

	var thing = false;

	function processThing(thing) {
		do.the.thing(thing);
	}
```

<Overview>
  # MDX Linting Demo

</Overview>
