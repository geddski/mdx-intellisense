import { Overview } from './sections/Overview'

# Linting Demo

1.  item one
2.  item two

```js
	const thing = true;

	if (thing) {
		console.log('thing');
	}

	const thing = false;

	function processThing(thing) {
		do.the.thing(thing);
	}
```

<Overview>
  # Hydrogen
</Overview>
